- [Canvas native addon fix](canvas-libuuid-fix.md) — libuuid.so.1 missing for canvas.node; symlink fix lives in node_modules, so re-apply after every npm install/rebuild.
- [Worker pool crash resilience](worker-pool-crash-resilience.md) — a dead worker_threads worker must not orphan its in-flight task, or the whole job hangs forever; add process-level crash isolation too.
- [Worker thread TypeScript loading](worker-ts-loading.md) — `--import tsx` fails for worker_threads; use `-r tsx/cjs` or eval bootstrap.
- [Sanity test design](sanity-test-design.md) — test must compare worker-to-worker (determinism), not main-thread vs worker (different pipelines).
- [RAM management](ram-management.md) — extraction queue limit must be dynamic (frame resolution × count); hardcoded 100 crashes 8 GB RAM with 4K video. Matching uses chunked path auto when estimated PreSet > 4 GB.
- [RAM-proportional constant scaling](ram-scaling-pattern.md) — replace a machine-size-tuned byte threshold with `os.totalmem() * multiplier` reproducing the original value, so it auto-scales on bigger machines.
- [Protected-file display-only fix](protected-file-display-only-fix.md) — fix a cosmetic bug in a locked-algorithm file by correcting only the final output, never the values internal decisions consume.
- [VLM verification concurrency](vlm-sequential-bottleneck.md) — VLM passes were sequential (100+ segments could take hours); parallelized via a worker pool bounded by the server's actual KV-cache slot count.
- [Imported dependency preservation](imported-dependency-preservation.md) — package installation can rewrite direct dependency ranges; restore the imported manifest when setup should stay minimal.
- [Match job stop semantics](match-job-stop-semantics.md) — stopping a background match job discards its result but can't preempt in-flight matching-engine/VLM work without touching protected files.
- [VLM alt-candidate full coverage](alt-candidate-full-coverage.md) — alt-candidates need a forced no-early-exit walk isolated from the protected first-pass walk, or they collapse to 1-frame stubs.
- [Candidate history dual purpose](candidate-history-dual-purpose.md) — StoredCandidateSet files now cover every segment, not just dropped ones; deferred recovery must filter by dropped+checked or it double-processes.
- [Pipeline frame-failure handling](pipeline-frame-failure-handling.md) — a rejected frame promise must not count as success or block the flush walk; total-failure must reject the job, not silently "complete" empty.
- [In-process VLM/candidate testing](vlm-candidate-test-pattern.md) — mock global.fetch + real ffmpeg frames beats a second HTTP server (ad hoc ports are unreliable here); set VLM_* env vars via shell, not in-file (import hoisting).
- [Real VLM endpoint testing](real-vlm-endpoint-testing.md) — background nohup servers die between tool calls (use a temp workflow instead); legacy /api/upload deletes source video, use /api/upload-chunk for Retry/VLM tests.
- [VLM concurrency deadlock audit](vlm-concurrency-deadlock-audit.md) — "VLM never starts" bug blamed the Semaphore; stress-tested with real sockets/latency/black-hole timeouts, found no leak/deadlock — likely infra (tunnel), not app code.
- [Retry verification split](retry-verification-split.md) — manual Retry (segment + gap) runs on `server/retry-verification/` (1:1 copy of `server/verification/`); make Retry-only changes there, never in the main verification system.
- [Candidate engine split](candidate-engine-split.md) — candidate-find system runs on `candidate-matching-engine.ts` (1:1 copy); `matching-engine.ts` is the protected main engine — never edit it for candidate behavior.
- [Crop-robust candidate ranking](crop-robust-candidate-ranking.md) — shorts are 9:16 crops from any horizontal position; candidate-side CLIP ranking tests full + left/center/right movie-frame crops (max cosine) so the true location is VLM-verified first; VLM prompt now leads with vertical-crop guidance. Reorder-only, fail-safe, candidate system only.
